# nodejs-api-gateway-implementation
# Registry.json holding configurations for api we want to forward our request.
# Command for calling apis in terminal = curl -X POST http://localhost:8000/testapi/dummybogusapi